import type { Member } from "./data"
const MEMBER_NICKNAMES: Record<string, readonly string[]> = { "member-cp": ["曹鹏", "本人"], "member-mom": ["妈妈", "母亲", "妈"], "member-child": ["小朋友", "孩子", "小孩"] }
function buildMemberHints(member: Member): string[] { const hints = new Set<string>([member.name, member.relationship, ...(MEMBER_NICKNAMES[member.id] ?? [])]); if (member.relationship === "母亲") { hints.add("妈妈"); hints.add("母亲") }; if (member.relationship === "父亲") { hints.add("爸爸"); hints.add("父亲") }; return [...hints].filter((h) => h.trim().length > 0) }
export function filterByMemberId<T extends { memberId: string }>(items: T[], memberId?: string): T[] { return memberId ? items.filter((i) => i.memberId === memberId) : items }
export function filterMembersById(members: Member[], memberId?: string): Member[] { return memberId ? members.filter((m) => m.id === memberId) : members }
export function resolveMemberFromHint(hint: string, members: Member[]): string | undefined { const n = hint.trim(); if (!n) return undefined; const byId = members.find((m) => m.id === n); if (byId) return byId.id; const l = n.toLowerCase(); return members.find((m) => m.name.toLowerCase() === l || m.relationship.toLowerCase() === l)?.id }
export function resolveMemberFromQuestion(question: string, members: Member[]): string | undefined { const n = question.trim(); if (!n) return undefined; const matches: Array<{ memberId: string; hintLength: number }> = []; for (const member of members) for (const hint of buildMemberHints(member)) if (hint.length >= 2 && n.includes(hint)) matches.push({ memberId: member.id, hintLength: hint.length }); if (!matches.length) return undefined; matches.sort((a, b) => b.hintLength - a.hintLength); return matches[0].memberId }
